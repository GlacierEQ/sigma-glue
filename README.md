# sigma-glue
Sigma Orchestrator — federation glue layer between Sigma UI and independent component repositories. Owns workflow state and sequencing; never credentials, approval authority, or provider gateways.
